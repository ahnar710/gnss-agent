# 路线图 — GNSS 文献调研 Agent

> 目标里程碑：M1 本地 MVP 可跑通最小闭环 → M2 可靠性加固 → M3 Windows 交付 → M4 商业/中文源插件 → M5 报告智能化

## M1：MVP 最小闭环（本期交付）
- [x] 产品设计文档（PRD / 架构 / 文献源矩阵 / 路线图）
- [x] 后端骨架：FastAPI + SQLite + 设置接口
- [x] LLM 客户端（OpenAI 兼容，重试退避）
- [x] 四源接入：OpenAlex / arXiv / Semantic Scholar / Crossref
- [x] 固定工作流：解析 → 检索 → 打分 → 深读 → 扩展 → 报告
- [x] 前端：设置 / 新建任务 / 实时进度 / 论文列表 / 报告查看 / 停止
- [x] 断点续跑 + 幂等（重启恢复 running 任务）
- [x] 小规模真实运行验证（目标篇数调小，验证闭环与报告产出）
- [ ] 内置 15 个示例主题（依赖 GNSS 词库研究结果）

## M2：可靠性加固（24h 连续运行）
- [x] 长时间压测：`scripts/stress_test.py`（压缩时间跑批，观测内存/DB 膨胀/限流，并按小时速率外推 24h 量级）
- [x] 检索预算与成本估算面板（LLM 调用/token/估算费用实时显示；单价可配；单任务成本上限保险丝）
- [x] 深读队列优先级调优（相关性 × 引用量对数 × 期刊白名单 × 近 3 年时效加成）
- [x] 报告增量更新（趋势与空白分析按已深读论文集合缓存，集合未变不重复调用 LLM）
- [x] 日志滚动与任务清理（每任务日志保留最近 2000 条；删除任务接口 + UI，级联清理论文/检索式/日志/报告）
- [ ] M2 后续：长跑真实模型验收（配合 M1 的真实 Key 任务一起做）

## M2.5：多 Agent 协作
- [x] 角色化拆分：策略官 / 评审员 / 深读员 / 分析师 / 主编(质控)，独立提示词与契约
- [x] 并行深读 Worker 池（原子认领、优雅停池、崩溃恢复、失败重试）
- [x] 每角色独立模型配置（成本分层）+ 主编质控开关
- [x] 协调器重构：编排器路由到角色；冒烟/压测适配通过
- [ ] 后续可选：边界论文双评审（Debate 轻量版）、角色级质控报表

## M3：Windows 交付（用户侧）
- [x] PyInstaller 打包（windowed 无黑窗口、版本资源、瘦身）+ Inno Setup 安装器（静默安装/桌面快捷方式/卸载，用户目录免 UAC）
- [x] 启动自检与单实例：端口占用自动换端口、已有实例只唤起浏览器、网络连通性检测、数据目录可写检查、错误日志 + 弹窗
- [x] 一键构建：`packaging/build_windows.bat`（Windows）与 `packaging/build.py`（跨平台）
- [x] Windows 验收测试清单与交付文档：`docs/WINDOWS_DELIVERY.md`
- [ ] 自动更新通道（可选：GitHub Release / 本地升级包）——设计见 WINDOWS_DELIVERY §5.2，建议 M4 再上

## M4：商业/中文源插件（P1/P2）
- [ ] CORE（免费 Key，全文）
- [ ] IEEE Xplore / Springer / Elsevier（用户机构 Key）
- [ ] 中文库评估报告（CNKI/万方合规与可行性结论，默认关闭，用户确认后开启）
- [ ] 全文下载与解析（开放获取 PDF → 正文级深读，需许可校验）

## M5：报告智能化
- [ ] 主题聚类可视化（子领域占比图、时间趋势图、引用网络图）
- [ ] 报告章节定制（用户可选：竞品对标 / 标准预研 / 算法选型模板）
- [ ] 对比综述（用户给 2 个主题，输出交叉对比）
- [ ] 引用导出（BibTeX / EndNote / RIS）
- [ ] 中文→英文摘要自动翻译版报告

## v0.2 已交付增强
- [x] **PDF 论文上传分析**：上传论文 PDF → 文本提取（pypdf）→ 直接进入深读队列（全文级深读）→ 进报告，来源标记「📄 用户上传」
- [x] **桌面化（免终端）**：macOS `.app`（BUNDLE，双击启动）+ Windows windowed exe（已有）；应用图标（雷达/卫星主题，ico/icns 自动生成）
- [x] 交付包重制：源码包（659KB）+ Mac 桌面版（46MB .app）
- [ ] 后续可选：上传 PDF 的 OCR 支持（扫描版）、引用条目提取（上传论文的参考文献自动加入检索）

## 风险跟踪
| 风险 | 触发信号 | 应对 |
|---|---|---|
| 24h 运行资源膨胀 | 内存/DB 增速异常 | M2 加入预算与清理 |
| 用户 Key 成本失控 | 单任务成本超预期 | 成本面板 + 深读批量降本 |
| 源限流加剧 | 检索吞吐下降 | 多源轮换 + 退避调参 |
| Windows 打包兼容问题 | 杀软误报/缺 VC 运行库 | 单目录打包 + 签名 + 安装说明 |
